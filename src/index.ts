import { IEntitySearch, IEnumerationRelated, IRelatedIds, IProperty, Sort, IResult } from "./model/model";
import { SearchType, TypeEntity} from "./model/SearchType";
 
export {
    IEntitySearch,
    IEnumerationRelated,
    IRelatedIds,
    IProperty,
    IResult,
    Sort,
    SearchType,
    TypeEntity
}
