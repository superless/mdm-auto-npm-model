import { IEntitySearch, IEnumerationRelated, IRelatedIds, IProperty, Sort, IResult } from "./model/model";

export {
    IEntitySearch,
    IEnumerationRelated,
    IRelatedIds,
    IProperty,
    Sort
}
